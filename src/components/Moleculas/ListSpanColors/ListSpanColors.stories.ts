import { Meta, StoryObj } from "@storybook/react"

import listSpanColors from './ListSpanColors'


const meta: Meta = {
    title: "Moleculas/Listacores",
    component: listSpanColors,
    argTypes: {
        
    }
};

export default meta;

type Story = StoryObj;

export const Normal: Story = {
    args: {
    }
};